const { DateTime, Duration } = require('luxon');

class Consulta {
    data;
    horaInicio;
    horaFim;
    duracao;
    #paciente;

    constructor(data, horaFim, duracao, horaInicio, paciente) {

    }
}